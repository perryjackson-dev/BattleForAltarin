import { expect, test, type Page } from '@playwright/test'
async function clean(page: Page) { await page.goto('/'); await page.evaluate(() => indexedDB.deleteDatabase('battle-for-altarin')); await page.reload(); await expect(page.getByText('Renderer ready')).toBeVisible() }
test('builds Cottage and Barracks on separate plots and restores both', async ({ page }) => { await clean(page); await expect(page.locator('canvas')).toHaveCount(1); await page.locator('canvas').click({ position: { x: 380, y: 105 } }); await page.getByRole('button', { name: /^Build$/ }).click(); await page.locator('canvas').click({ position: { x: 595, y: 105 } }); await page.getByRole('button', { name: /^Barracks/ }).click(); await page.getByRole('button', { name: /^Build$/ }).click(); await expect(page.getByText('Wood: 750')).toBeVisible(); await expect(page.getByText('Stone: 850')).toBeVisible(); await expect(page.getByText(/Cottage —/)).toBeVisible(); await expect(page.getByText(/Barracks —/)).toBeVisible(); await expect(page.getByText('Population Capacity: 50')).toBeVisible({ timeout: 15000 }); await page.reload(); await expect(page.getByText('Population Capacity: 50')).toBeVisible() })
test('refresh during construction preserves progress and resources', async ({ page }) => { await clean(page); await page.locator('canvas').click({ position: { x: 380, y: 105 } }); await page.getByRole('button', { name: /^Build$/ }).click(); await page.reload(); await expect(page.getByRole('status')).toContainText('Cottage'); await expect(page.getByText('Wood: 900')).toBeVisible(); await expect(page.getByText('Stone: 950')).toBeVisible(); await expect(page.getByText('Population Capacity: 0')).toBeVisible(); await expect(page.getByText('Population Capacity: 50')).toBeVisible({ timeout: 15000 }) })
