import { expect, test, type Page } from '@playwright/test'
async function clean(page: Page) { await page.goto('/'); await page.evaluate(() => indexedDB.deleteDatabase('battle-for-altarin')); await page.reload(); await expect(page.getByText('Renderer ready')).toBeVisible() }
test('builds Cottage and Barracks on separate plots and restores both', async ({ page }) => { await clean(page); await page.locator('canvas').click({ position: { x: 380, y: 105 } }); await page.getByRole('button', { name: /^Build$/ }).click(); await page.locator('canvas').click({ position: { x: 595, y: 105 } }); await page.getByRole('button', { name: /^Barracks/ }).click(); await page.getByRole('button', { name: /^Build$/ }).click(); await expect(page.getByText('Wood: 4750')).toBeVisible(); await expect(page.getByText('Stone: 4850')).toBeVisible(); await expect(page.getByText('Population Capacity: 50')).toBeVisible({ timeout: 15000 }); await page.reload(); await expect(page.getByText('Population Capacity: 50')).toBeVisible() })
test('refresh during construction preserves progress and resources', async ({ page }) => { await clean(page); await page.locator('canvas').click({ position: { x: 380, y: 105 } }); await page.getByRole('button', { name: /^Build$/ }).click(); await page.reload(); await expect(page.getByRole('status')).toContainText('Cottage'); await expect(page.getByText('Wood: 4900')).toBeVisible(); await expect(page.getByText('Stone: 4950')).toBeVisible(); await expect(page.getByText('Population Capacity: 0')).toBeVisible(); await expect(page.getByText('Population Capacity: 50')).toBeVisible({ timeout: 15000 }) })
test('upgrades a completed Cottage and restores an in-progress upgrade', async ({ page }) => { await clean(page); await page.locator('canvas').click({ position: { x: 380, y: 105 } }); await page.getByRole('button', { name: /^Build$/ }).click(); await expect(page.getByText('Population Capacity: 50')).toBeVisible({ timeout: 15000 }); await page.locator('canvas').click({ position: { x: 380, y: 105 } }); await expect(page.getByRole('dialog', { name: 'Upgrade building' })).toContainText('Level 1 / Rank I'); await page.getByRole('button', { name: 'Upgrade' }).click(); await expect(page.getByRole('status')).toContainText('Cottage L1'); await expect(page.getByText('Wood: 4800')).toBeVisible(); await page.reload(); await expect(page.getByRole('status')).toContainText('Cottage L1'); await expect(page.getByText('Population Capacity: 50')).toBeVisible(); await expect(page.getByText('Population Capacity: 70')).toBeVisible({ timeout: 15000 }); await page.reload(); await expect(page.getByText('Population Capacity: 70')).toBeVisible() })
