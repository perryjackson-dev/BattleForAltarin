import { PixiViewport } from './game/rendering/PixiViewport'
import { applicationMetadata } from './schemas/application'
import { useGameStore } from './state/gameStore'
const views = ['city', 'field', 'world'] as const
export default function App() { const activeView = useGameStore((state) => state.activeView); const selectView = useGameStore((state) => state.selectView); const metadata = applicationMetadata.parse({ title: 'Battle for Altarin' }); return <main className="mx-auto flex min-h-screen max-w-5xl flex-col gap-6 bg-slate-950 px-6 py-10 text-slate-100"><header className="border-b border-slate-700 pb-6"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-slate-400">Browser strategy game</p><h1 className="mt-2 text-3xl font-bold">{metadata.title}</h1></header><nav aria-label="Game views" className="flex gap-2">{views.map((view) => <button className={`rounded px-4 py-2 capitalize ${activeView === view ? 'bg-slate-100 text-slate-950' : 'bg-slate-800 text-slate-200'}`} key={view} onClick={() => selectView(view)} type="button">{view}</button>)}</nav><section aria-labelledby="renderer-title" className="space-y-3"><div><h2 id="renderer-title" className="text-lg font-semibold capitalize">{activeView} view</h2><p className="text-sm text-slate-400">PixiJS renderer initialisation check.</p></div><PixiViewport /></section></main> }
