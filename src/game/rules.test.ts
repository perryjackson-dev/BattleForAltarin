import { describe, expect, it } from 'vitest'
import { initialGameState, plotIds, startConstruction, startUpgrade, resolveConstructions, isBuildQueueBusy, derivePopulationCapacity } from './rules'
describe('City foundation rules',()=>{
 it('starts with 24 plots, dedicated structures, and resources',()=>{expect(plotIds).toHaveLength(24);expect(Object.keys(initialGameState.city.plots)).toHaveLength(24);expect(initialGameState.wood).toBe(5000);expect(initialGameState.city.castle).toMatchObject({buildingId:'castle',level:1})})
 it('enforces one shared build queue and storehouse limit',()=>{const a=startConstruction(initialGameState,'city_plot_01','storehouse',0);expect(isBuildQueueBusy(a)).toBe(true);expect(startConstruction(a,'city_plot_02','cottage',1)).toBe(a);const b=resolveConstructions(a,20000);expect(startConstruction(b,'city_plot_02','storehouse',1)).toBe(b)})
 it('upgrades plots and dedicated structures with current population retained',()=>{const built=resolveConstructions(startConstruction(initialGameState,'city_plot_01','cottage',0),10000);const up=startUpgrade(built,'city_plot_01',10000);expect(derivePopulationCapacity(up)).toBe(50);const done=resolveConstructions(up,25000);expect(done.city.plots.city_plot_01).toMatchObject({status:'occupied',level:2});const castle=startUpgrade(done,'castle',30000);expect(castle.city.castle).toMatchObject({currentLevel:1,targetLevel:2})})
})
