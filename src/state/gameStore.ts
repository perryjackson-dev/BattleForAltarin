import { create } from 'zustand'
import { initialGameState, resolveConstructions, startConstruction, type GameStateData } from '../game/rules'
import { loadGameState, saveGameState } from '../persistence/save'
export type GameView = 'city' | 'field' | 'world'
type GameState = GameStateData & { activeView: GameView; hydrated: boolean; selectView: (view: GameView) => void; hydrate: () => Promise<void>; beginConstruction: (plotId: string, buildingId: string) => Promise<void>; resolve: () => Promise<void> }
export const useGameStore = create<GameState>((set, get) => ({ ...initialGameState, activeView: 'city', hydrated: false, selectView: (activeView) => set({ activeView }), hydrate: async () => { const loaded = await loadGameState(); const resolved = resolveConstructions(loaded ?? initialGameState, Date.now()); set({ ...resolved, hydrated: true }); await saveGameState(resolved) }, beginConstruction: async (plotId, buildingId) => { const next = startConstruction(get(), plotId, buildingId, Date.now()); set(next); await saveGameState(next) }, resolve: async () => { const current = get(); const next = resolveConstructions(current, Date.now()); if (next !== current) { set(next); await saveGameState(next) } } }))
